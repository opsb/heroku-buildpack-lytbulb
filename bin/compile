#!/usr/bin/env bash

set -e            # fail fast
set -o pipefail   # don't ignore exit codes when piping output
# set -x          # enable debugging

# Configure directories
build_dir=$1
cache_dir=$2
env_dir=$3

addApi()
{
	echo "  >> Copying api to build"
	cp -rf $build_dir/api/* $build_dir	
}

addApi
exit 0